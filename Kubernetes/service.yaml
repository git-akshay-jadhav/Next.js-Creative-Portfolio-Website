apiVersion: v1
kind: Service
metadata:
  name: Portfolio
  labels:
    app: Portfolio
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: Portfolio
